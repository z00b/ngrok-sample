version: 2.1

jobs:
  network:
    docker:
      - image: cimg/python:3.9.2

    working_directory: ~/repo

    steps:
      - checkout

      - run:
          name: install dependencies
          command: |
            sudo apt-get update
            sudo apt-get install ngrok

      - run:
          name: spin up server
          command: echo "spinning up"

workflows:
  version: 2
  do_networking:
    jobs:
      - network
